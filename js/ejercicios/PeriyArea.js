const largo = Number(prompt("ingresa el largo del rectangulo"))
const ancho = Number(prompt("ingresa el ancho del rectangulo"))

const P = 2*(largo + ancho)

const A = largo*ancho

alert("El perimetro del rectangulo es " + P + "[m] y el area es igual a " + A + "[m^2]")